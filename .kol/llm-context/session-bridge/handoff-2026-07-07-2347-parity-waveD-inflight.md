# Handoff — 2026-07-07 23:47

## Goal of the current arc
Close the full labs→editor parity backlog ("fix all of it") in waves. Supersedes handoff-2026-07-07-2316. Earlier same-day work (internal audit + 4 fix waves) is logged in `session-log/2026-07-07-full-audit-four-fix-waves.md`; the parity audit that drove this arc produced ~200 missing-feature rows (9 systemic + 11 big-ticket).

## Status of the wave plan
- **Wave A (randomize architecture + filter stacking): DONE.** Scoped seeded randomize (rolls.jsx + rng.js), motion Frame/Form presets, field-camera rail; filter chains (filterChain.js, `layer.filters[]` cap 8, EffectsPanel rewrite, sweep stacks + presets, engine filters on loop layers).
- **Wave B (schema exposures, transport, camera/keyframes): DONE.** ~30 schema params unhidden; viewport motion layer (loops/lib/viewport.js); transport resetEpoch governs sims + video (playbackRate/videoLoop/videoMuted); generic cameraKeys drag-orbit; host.js unpinned (keyframes/wireframe/grid-of-9/duration integer-cycles via loops/gl/phase.js); KeyframeEditor + CameraPoseSlots; penrose generate-tab + dim-opacity=5 fix; lookPresets.js.
- **Wave C (authoring sub-editors): DONE + BUILT GREEN.** PC1 pattern (RulesEditor, ProfileEditor, glyph/custom-SVG tiles), PC2 kinetic tier-2 (VF axes, OpenType menu, motion stack, custom-path editor, grouping, on-canvas KineticElementOverlay via DOM seam), PC4 math (mathfn.js free-text exprs, CurveEditor, BindDot expr plot, axes overlays), PC6 para-type (specimen grid, guides/anatomy, flatten-to-vector). Mount specs applied by orchestrator: CurveEditor + ParatypeTools in ParametersPanel; flattenParatype in state.jsx (both context value lists). **`pnpm build` GREEN — 2840 modules, zero errors** — this is the last known-good checkpoint.
- **Wave D (softforms scene edit / video tooling / batch export / gamepad): IN FLIGHT.**
  - **PD-gamepad DONE** (self-wired, no mount spec): 16 gamepad sources + derived stick angle/force, gamepad learn, binding response-shaping (invert→smooth→curve→remap, all sources), `curve` is the only new op, defaults identity. Task-4 button→action mapping DEFERRED — design note in its report (needs unowned shell files; wire via kol:* events: gamepadActions.js hook + kol:reroll/kol:snapshot listeners).
  - **PD-export DONE** (self-wired EditorFooter Output tab): batch multi-size PNG zip matrix (NEW lib/zipStore.js store-only zip, `svgToPngBlob`/`renderComposePngBlob` factored out, BatchExportModal), 3:5 (1080×1800) + 5:3 (1800×1080) aspects added, live real-time webm Record (re-composites via build/snapshot path into a scratch canvas + captureStream — no single live canvas exists). No ParametersPanel spec. Zip validated vs system unzip.
  - **PD-video DONE** but with a CROSS-AGENT BLOCKER on video crop. Shipped in its own files: trimIn/trimOut params (photo.js) wired into all 3 video paths + transport-rewind→trimIn; VideoPhotoLayer crop render; NEW lib/clipStore.js (IndexedDB, `saveClip` at EditorFooter onPickVideo, `hydrateVideoClips` in state.jsx draft-restore); crop ungated in ToolPalette + LayerInspector. Trim UI = the two auto-rendered PHOTO_SCHEMA sliders (no ParametersPanel spec). **VIDEO CROP IS NON-FUNCTIONAL until 3 patches to PD-soft-owned files land — apply on resume:** (1) CanvasArea `enterCrop` needs a `srcType==='video'` branch using `<video>`+loadedmetadata for rect init (full patch in PD-video's transcript); (2) CanvasArea remove two `&& l.srcType !== 'video'` guards (~:807, ~:848, match by text — lines drift); (3) CropOverlay.jsx:153 ghost `<img>` → `<video muted>` when video; (4) build.js photoLayerSvg video branch (~:154) honor `imgW` crop via clipPath + toDataURL (full patch in transcript), drop the stale "Crop never applies to video" comment. clipStore caveats: `deleteClip` unwired (minor leak on clear/delete); duplicated video layers share one objectURL, dupe won't restore on reload.
  - **PD-soft DONE** (last Wave-D agent — ALL WAVE D NOW COMPLETE). No engine/catalog/host change needed — `layer.forms` was already engine-consumed. Shipped NEW SoftformsLayers.jsx (per-form list: type/reorder/dup/delete/add cap 5, transforms x/y/sx/sy/rot/hue[+z/sz], forms-scoped randomizers Color/Transform/Scale/Animation/Rearrange), NEW SoftformsHandleOverlay.jsx (2D SDF hit-test on-canvas handles, kinetic-overlay pattern, rotation-compensated), CanvasArea wiring (`kol:softform-edit`/`kol:softform-select` events, mirrors kinetic). 3D on-canvas handles skipped by design (labs uses orbit-drag). ParametersPanel mount spec: `import SoftformsLayers from './SoftformsLayers'` + inside the `tab==='generate'` block first child: `{(layer.loopId === 'softforms' || layer.loopId === 'softforms3d') && <SoftformsLayers layer={layer} />}`.

## RESUME UPDATE (post-tmux-crash, new session f4356245): Wave D APPLIED + BUILT GREEN; Wave E IN FLIGHT
- All Wave-D specs applied by orchestrator: SoftformsLayers mount in ParametersPanel; video-crop patches (CanvasArea enterCrop video branch + both `srcType !== 'video'` guards removed + CropOverlay ghost `<video>` branch + build.js video imgW-crop branch, stale comment dropped). Reconciled cleanly with PD-soft's CanvasArea softforms wiring. **`pnpm build` GREEN — 2846 modules, zero errors.** New checkpoint.
- **Wave E IN FLIGHT (2 no-dep agents launched):** E1 = OS-file drag-drop onto stage + webcam source + mirror + MediaPicker folder-drilldown/lightbox/copy-URL (owns CanvasArea/state/LayerRenderer/MediaPicker/photo.js/EditorFooter). E2 = scanline live filter surface + distort cursor record/replay + appSettings global defaults (owns scanline.js/distortionEngine/catalog/theme.js/Editor.jsx/MenuTop + NEW appSettings.js).
- **PIXI GPU FILTER TIER = FLAGGED, NOT BUILT** — the one Wave-E item needing a new heavyweight dep (`pnpm add pixi.js @pixi/filters`). Provisioning is the user's; do NOT `pnpm add`. On resume: ask the user to run the install, then build the ~33-fx tier as a lazy chunk of filter-chain stages. Until then it's the sole remaining parity gap besides the 3 already-flagged app-sized items (Interfaces, 3D Lens, server pipelines).
- **Next:** collect E1+E2 completions, apply any mount specs, `pnpm build` + `pnpm build:lib`, then /log-work + report.

## Wave D — specs to apply on resume (SUPERSEDED — all applied, see RESUME UPDATE above)
1. **ParametersPanel.jsx**: add SoftformsLayers mount (PD-soft spec above). (Gamepad/export/video needed no ParametersPanel mount — trim = auto PHOTO_SCHEMA sliders.)
2. **Video crop cross-agent patches** (PD-video specs, now unblocked since PD-soft's CanvasArea edits landed — RECONCILE by matching text, PD-soft added softforms wiring to the same file): CanvasArea `enterCrop` video branch; remove two `&& *.srcType !== 'video'` guards; CropOverlay.jsx:153 ghost `<img>`→`<video muted>`; build.js photoLayerSvg video branch honor `imgW` (full patches in PD-video transcript / handoff section above).
3. `pnpm build` — first Wave-D build, expect cross-agent breakage (two agents edited CanvasArea: PD-soft softforms overlay + the video-crop guard removals — reconcile).
4. Then Wave E, then final `pnpm build` + `pnpm build:lib`, then /log-work, then report.
NOTE: PD-video + PD-soft BOTH edited CanvasArea.jsx (softforms overlay wiring + video-crop guards) — the file on disk has PD-soft's version; the video-crop guard removals still need applying and must not clobber PD-soft's `softformsEdit` additions.
- **Wave E (pending, NOT started):** OS-file drag-drop onto stage, webcam source + mirror, MediaPicker drill-down/lightbox/copy-URL; pixi GPU filter tier (~33 fx, LAZY chunk — needs `pnpm add`); stragglers (scanline live filter surface, distort cursor record/replay, appSettings global defaults). Then final builds + /log-work + report (tasks #11, #12 in tracker).

## Next intended action (on resume)
1. Collect the 3 remaining Wave-D completion notifications (PD-soft, PD-video, PD-export); apply their ParametersPanel/build.js/EditorFooter mount+patch specs.
2. `pnpm build` — fix cross-agent breakage (Wave D unbuilt).
3. Launch Wave E, then final `pnpm build` + `pnpm build:lib`, then **/log-work** (user asked "log when done"), then status report.

## Open decisions / caveats
- Flagged OUT of scope (user accepted): Interfaces composer, Radar 3D Lens scene, server ffmpeg/poster pipelines.
- Seed-field styling: PA2 (EffectsPanel) built its own seed row before PA1's SeedField existed — two styles, unify later.
- Scanline geometry/mark selects roll within scope (generic model) vs labs' pinned category — user may want pinned; flagged in PA1 report.
- ParametersPanel.jsx is the perennial contention hotspot — one owner per wave, everyone else hands back mount specs.
- KOL package bumps still deferred (component 0.1.2→0.4.0 etc.).
- Meta fields on layers (all survive JSON save/load, none render as controls): `_rollSeed`, `_framePreset/_formPreset/_lookPreset`, `_camSlots`, `_xyX/_xyY`.

## Working memory
- Fleet convention that's held all session: disjoint file ownership per parallel agent; exact keys/exports fixed as contracts in prompts; cross-owned lines become "patch specs" the orchestrator applies inline; ParametersPanel + state.jsx never edited by a parallel agent.
- Build has been green after every wave so far on first try — the discipline works; keep it.
- Pause reason: user at ~90% usage, needed space. The 3 in-flight Wave-D agents were left running (killing mid-write risks an unbuildable tree; post-Wave-C green build is the clean recovery point regardless).
